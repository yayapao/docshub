---
id: vscode
title: ''
sidebar_label: vscode
---

import { DocsHeader } from '/src/components/Layout/DocsHeader'
import {
  HighlightWithCode,
  HighlightWithText,
} from '/src/components/Highlights/TextContent'
import { ResolvedTag } from '/src/components/Highlights/CustomizedTag'

<DocsHeader
  title="vscode"
  description="Visual Studio Code is a lightweight but powerful source code editor."
  tags={['Editor']}
  links={[
    {
      label: `Redefined Editor?`,
      link: 'https://code.visualstudio.com/docs',
    },
    {
      label: 'Remote development over SSH',
      link: 'https://code.visualstudio.com/docs/remote/ssh-tutorial',
    },
  ]}
/>

<!-- -->

## Debugger Nest.js App

VSCode 支持 N 种方式进行 debug

- 设置 `Debug: Toggle Auto Attach` 为 `smart`，当每次打开 vscode 的 ternimal 时，会自动进入 debug 模式
- 通过 `Debug: Create JavaScript Debug Terminal` 创建一个命令行窗口，在其内部执行命令，从而进行 debug
- 配置 `launch.json` 来编写 debug 脚本，在 vscode debug 模块内启动

### Attach vs Launch

Attach: 依附当前进程

Launch: 另起一个新的进程

## Remote Development Over SSH

:::note

- 安装官方插件 - `remote SSH`
- 配置本地/服务器 SSH，参考 [SSH 远程登录](https://doc.joyjoy.cc/docs/server/cloudServer#%E8%AE%BE%E7%BD%AE-ssh-%E8%BF%9C%E7%A8%8B%E7%99%BB%E5%BD%95)

:::
